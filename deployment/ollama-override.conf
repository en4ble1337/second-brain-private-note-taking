# systemd drop-in for ollama.service
# Ensures Ollama binds to localhost only — not exposed on the LAN.
# Place at: /etc/systemd/system/ollama.service.d/override.conf

[Service]
Environment="OLLAMA_HOST=127.0.0.1:11434"
